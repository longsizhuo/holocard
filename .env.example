# 模型权重来源。默认走 Hugging Face 官方 CDN，不需要自己托管，也没有带宽账单。
# 只有在 huggingface.co 访问不了（如中国大陆）或想自己掌控分发时才需要改。

# 国内镜像
# VITE_MODEL_HOST=https://hf-mirror.com/

# 自建镜像（R2 / OSS / jsDelivr 等），路径模板要和你的目录结构对上
# VITE_MODEL_HOST=https://cdn.example.com/
# VITE_MODEL_PATH_TEMPLATE={model}/resolve/{revision}/

# 强制权重精度。留空自动选：WebGPU 用 fp16（约 50MB），WASM 用 q8（约 25MB）。
# 想把下载量压到最小可以强制 q8，代价是深度图细节略糊。
# VITE_MODEL_DTYPE=q8
